import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    button: {
        width: 24,
        height: 24,
        borderRadius: 24 / 2,
        borderColor: '#128085',
        borderWidth: 2,
        alignItems: 'center',
        justifyContent: 'center',
    },
    dot: {width: 14, height: 14, borderRadius: 16 / 2},
});
