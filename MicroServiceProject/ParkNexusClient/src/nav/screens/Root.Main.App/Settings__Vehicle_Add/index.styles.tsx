import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    wrapper: {
        paddingHorizontal: 16,
    },

    formFieldLabel: {
        fontSize: 14,
        fontWeight: "600",
        marginBottom: 4,
    },
    vehicleTypeLabel: {
        fontSize: 14,
        fontWeight: "600",
    },

    saveButton: {
        marginTop: 16,
        marginBottom: 16,
    },
});
